# Newsletter Builder

This file is for Cursor and other coding agents. Claude Code reads `CLAUDE.md` for the same rules.

Premium work must follow `.cursor/skills/newsletter-premium/SKILL.md`. Feature names and status rules are in `.cursor/skills/newsletter-premium/product.md`.

Do not remove or rename wizard steps, review buckets, voice controls, multi-source search, or the Mailchimp confirm step. Add new behavior beside them.

`apps/web` is the Next.js UI. `apps/api` is the FastAPI server. The browser talks only to `/backend/...`.
